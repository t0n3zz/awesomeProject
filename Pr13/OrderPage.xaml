<Page x:Class="Pr13.OrderPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Pr13"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="OrderPage">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Оформление заказа" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock Text="ФИО:"/>
            <TextBox x:Name="TxtName" Margin="0,5"/>
            <TextBlock Text="Почта:"/>
            <TextBox x:Name="TxtEmail" Margin="0,5"/>
            <TextBlock Text="Адрес доставки:"/>
            <TextBox x:Name="TxtAddress" Margin="0,5" Height="50" TextWrapping="Wrap"/>
        </StackPanel>

        <GroupBox Header="Ваш заказ" Grid.Row="2">
            <ListBox x:Name="FinalItemsList" BorderThickness="0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name, StringFormat='• {0}'}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </GroupBox>

        <StackPanel Grid.Row="3" Margin="0,15,0,0">
            <TextBlock x:Name="TxtTotal" FontSize="18" FontWeight="Bold" Foreground="Green"/>
            <Button Content="ПОДТВЕРДИТЬ И ЗАКАЗАТЬ" Click="ConfirmOrder_Click" 
            Background="DarkGreen" Foreground="White" Height="50" FontWeight="Bold" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Page>
