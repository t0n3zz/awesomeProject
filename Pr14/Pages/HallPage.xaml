<Page x:Class="Pr14.Pages.HallPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Pr14.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="HallPage">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel HorizontalAlignment="Center">
            <TextBlock x:Name="TxtTitle" FontSize="22" FontWeight="Bold" Text="Выбор мест"/>
            <TextBlock x:Name="TxtSessionInfo" HorizontalAlignment="Center" Foreground="Gray"/>
        </StackPanel>

        <Border Grid.Row="1" Background="LightGray" Height="10" Margin="50,20" CornerRadius="5">
            <TextBlock Text="ЭКРАН" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10"/>
        </Border>

        <ItemsControl x:Name="LbxSeats" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="5" Rows="5"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Content="{Binding Number}" 
                        Width="50" Height="50" Margin="5"
                        Background="{Binding Color}"
                        IsEnabled="{Binding IsAvailable}"
                        Click="Seat_Click"
                        Tag="{Binding}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Назад" Click="Back_Click" Width="100" Margin="10,0"/>
        </StackPanel>
    </Grid>
</Page>
